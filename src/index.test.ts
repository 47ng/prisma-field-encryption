import lib from './index'

describe('typescript-library-starter', () => {
  test('testing works', () => {
    expect(lib).toBe(
      'https://twitter.com/fortysevenfx/status/1463265166682898438'
    )
  })
})
